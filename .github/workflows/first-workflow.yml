name: A work flow for my Hello world file
on: push
jobs:
  build:
     name: Hello world action
     runs-on: ubuntu-latest
     steps:
     - uses: actions/checkout@v4.2.2
     - uses: ./actions-a
       with: 
         MY_NAME: Ashok
     
